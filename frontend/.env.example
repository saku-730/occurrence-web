# Next.js server-side rewrite destination. Do not expose this as NEXT_PUBLIC_*.
BACKEND_URL=http://127.0.0.1:3001

# Optional MapLibre style URL override.
# If omitted, /map uses OpenFreeMap Liberty (OpenStreetMap-derived vector map data).
NEXT_PUBLIC_MAP_STYLE_URL=
