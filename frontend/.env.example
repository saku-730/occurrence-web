# Next.js server-side rewrite destination. Do not expose this as NEXT_PUBLIC_*.
BACKEND_URL=http://127.0.0.1:3001

# Optional MapLibre style URL. If omitted, /map uses the MapLibre demo style for development.
# Set this to the production tile/style provider before production deployment.
NEXT_PUBLIC_MAP_STYLE_URL=
